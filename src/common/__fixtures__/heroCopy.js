// PRD-authoritative hero copy. Tests assert the rendered HeroBanner matches
// these exact strings, so keep this as the single source of expected copy
// shared across specs (see issue #52 acceptance criteria).
export const HERO_HEADLINE = 'Online Casino';
export const HERO_SUPPORTING = 'Browse our game catalog';
export const HERO_CTA_LABEL = 'Browse games';
