// PRD-authoritative hero copy. This is the single source of truth for the hero
// headline, supporting line, and CTA label: HeroBanner.vue renders these exact
// values and the specs assert against the same constants, so the copy can only
// be changed in one place (see issue #52).
export const HERO_HEADLINE = 'Online Casino';
export const HERO_SUPPORTING = 'Browse our game catalog';
export const HERO_CTA_LABEL = 'Browse games';
